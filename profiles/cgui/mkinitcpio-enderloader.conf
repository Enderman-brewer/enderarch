# Enderloader mode initrd — recovery TUI

HOOKS=(base enderloader-init)
# enderloader-init provides: /lib/enderarch/init-mode (recovery TUI script)

BINARIES=()
FILES=()

COMPRESSION="zstd"
COMPRESSION_OPTIONS=(-19)