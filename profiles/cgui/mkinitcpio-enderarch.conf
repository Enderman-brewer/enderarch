# Enderarch mode initrd — lightweight, just the live boot logic

HOOKS=(base enderarch-init)
# enderarch-init provides: /lib/enderarch/init-mode (live boot script)

BINARIES=()
FILES=()

COMPRESSION="zstd"
COMPRESSION_OPTIONS=(-19)